/*
题意：
给定一个非负整数数组 nums ，你最初位于数组的第一个下标。
数组中的每个元素代表你在该位置可以跳跃的最大长度。
计算你到达最后一个下标所需的最小跳跃次数。

数据范围：
1 <= nums.length <= 10^4
0 <= nums[i] <= 1000
*/


class Solution {
public:
    int jump(vector<int>& nums) {

    }
};
